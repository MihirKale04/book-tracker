<% const title = escapeHtml(book.title); %>
<%- include("../layout", { title, body: `
  <div class="book-detail">
    <h2>${escapeHtml(book.title)}</h2>
    <p class="author muted">by ${escapeHtml(book.author)}</p>
    <div class="meta">
      <span class="badge ${book.status}">${book.status}</span>
      ${book.rating ? `<span class="rating">⭐ ${book.rating}/5</span>` : `<span class="muted">No rating yet</span>`}
    </div>
  </div>
  
  <div class="book-actions">
    <a href="/books/${book.id}/edit" class="btn btn-primary">✏️ Edit</a>
    <form method="post" action="/books/${book.id}/delete" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this book?');">
      <button type="submit" class="btn btn-danger">🗑️ Delete</button>
    </form>
    <a href="/books" class="btn btn-secondary">← Back to All Books</a>
  </div>
` }) %>

